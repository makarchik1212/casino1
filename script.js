function goToCrash() {
  alert("Переход в режим Crash (пока не сделано)");
  // Здесь потом будет логика перехода
}

function goToMines() {
  alert("Переход в режим Mines (пока не сделано)");
  // Здесь потом будет логика перехода
}

// Будущая логика для управления звездами
let stars = 1000;

function updateStars() {
  document.getElementById('stars').textContent = stars;
}

